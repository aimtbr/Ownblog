

const initialState = {
    posts: [],
    isLoading: false,
    error: false,
};

const Posts = (state=initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
};